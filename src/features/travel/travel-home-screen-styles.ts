import { StyleSheet } from 'react-native';

import { spacing } from '@/design-system';

export const travelHomeScreenStyles = StyleSheet.create({
  fill: { flex: 1 },
  transparentScreen: {
    backgroundColor: 'transparent',
  },
  screen: { gap: spacing.md },
});
