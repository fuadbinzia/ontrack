import { TrackersScreen } from '@/features/trackers/trackers-screen';

/** More tab — pin/reorder which trackers appear in the bottom nav. */
export default function TrackersRoute() {
  return <TrackersScreen />;
}
